production:
    rails:
        server:
            unique_name: frontend
        configuration:
            do_initial_db_schema_load: true
development:
    rails:
        server:
            unique_name: frontend
        configuration:
            do_initial_db_schema_load: true